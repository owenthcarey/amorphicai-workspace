describe("Core module generic test", () => {
  it("Should be true", () => {
    expect(true).toBeTruthy();
  });
});

/*
import { AmorphicaiWorkspaceCoreModule } from './core.module';

describe('AmorphicaiWorkspaceCoreModule', () => {
  it('should work', () => {
    expect(new AmorphicaiWorkspaceCoreModule(null)).toBeDefined();
  });
});
*/
